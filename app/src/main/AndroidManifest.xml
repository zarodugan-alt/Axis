<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Haptics: performHapticFeedback needs nothing, the Vibrator pulses do. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- BYOK: every model request goes straight from the device to the
         provider the user configured. No AXIS server exists. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Voice input (SpeechRecognizer) — optional, requested at first use. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Flashlight tool + camera permission for the torch callback. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <!-- Do Not Disturb control for routines and the DND tool. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Runtime notification permission on API 33+ (harmless below). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      The launcher must see every launchable app. `<queries>` (not
      QUERY_ALL_PACKAGES) keeps the visibility surface to exactly what a
      launcher needs.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.WEB_SEARCH" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
    </queries>

    <application
        android:name=".AxisApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Axis"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Keeps the drawer + search index live. ACTION_PACKAGE_* broadcasts
             are exempt from background-delivery limits, so a manifest receiver
             is correct here (no polling service needed). -->
        <receiver
            android:name=".drawer.PackageChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- P2 · notification listener: captures, triages and holds
             notifications. Bound by the system, granted explicitly by the
             user in Notification access. -->
        <service
            android:name="axis.sense.notify.AxisNotificationListenerService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- P2 · accessibility service: foreground app + window title context.
             No text capture, no gesture injection. -->
        <service
            android:name="axis.sense.screen.AxisAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            tools:ignore="AccessibilityPolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/axis_accessibility_service" />
        </service>
    </application>
</manifest>
