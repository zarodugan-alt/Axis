<?xml version="1.0" encoding="utf-8"?>
<!--
  AXIS accessibility config (spec P2): window-state events only, no text
  capture, no gesture injection, no filtering by package. The service reports
  the foreground package and window title — nothing else.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_description"
    android:notificationTimeout="200"
    android:summary="@string/accessibility_description" />
